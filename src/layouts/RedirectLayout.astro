---
import Head from "../components/Head.astro";
import {
  DEFAULT_TITLE,
  DEFAULT_DESCRIPTION,
  DEFAULT_AUTHOR,
  DEFAULT_DATE,
  DEFAULT_KEYWORDS,
  DEFAULT_IMAGE,
} from "../consts";

const { title = DEFAULT_TITLE, to } = Astro.props;
---

<html lang="en">
  <head>
    <meta http-equiv="refresh" content={"0;URL='" + to + "'"} />
  </head>
</html>
